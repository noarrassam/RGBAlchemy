export const enum EGameStatus {
    INITIAL,
    PROGRESS,
    FINISHED,
};

export const enum ECellType {
    EMPTY,
    OUTER,
    INNER,
};

export const enum EPosition {
    LEFT,
    TOP,
    RIGHT,
    BOTTOM
};